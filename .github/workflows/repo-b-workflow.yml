name: Deployment
 
on:
  repository_dispatch:
    types: [ deploy ]

env:
  PERSONAL_TOKEN: ${{ github.event.client_payload.token }}
  BRANCH_NAME: ${{ github.event.client_payload.branch }}
  EMAIL_USERNAME: ${{ github.event.client_payload.email_user }}
  EMAIL_PASSWORD: ${{ github.event.client_payload.email_pass }}

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v3
        with:
          repository: samah603/import-repo
          token: ${{ env.PERSONAL_TOKEN }}
          persist-credentials: true
          ref: ${{ env.BRANCH_NAME }}
          
      - name: Echo env
        run: |
         echo "EMAIL_USERNAME=${{ env.EMAIL_USERNAME }}"
         echo "EMAIL_USERNAME=${{ env.EMAIL_PASSWORD }}"
